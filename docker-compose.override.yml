# Docker Compose override for GitHub Codespaces
# This file is automatically used when running in Codespaces

services:
  frontend:
    build:
      args:
        # Use Codespaces URL for backend API
        VITE_API_BASE_URL: https://${CODESPACE_NAME}-8000.app.github.dev/api/v1
